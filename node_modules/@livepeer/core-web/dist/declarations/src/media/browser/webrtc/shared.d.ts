import { AccessControlParams, AudioTrackSelector, VideoTrackSelector } from '@livepeer/core';
/**
 * Checks if WebRTC is supported.
 */
export declare const isWebRTCSupported: () => boolean;
export declare function createPeerConnection(host: string | null): RTCPeerConnection | null;
export type WebRTCVideoConfig = {
    /**
     * The timeout of the network requests made for the SDP negotiation, in ms.
     *
     * @default 20000
     */
    sdpTimeout?: number;
    /**
     * Disables the speedup/slowdown mechanic in WebRTC, to allow for non-distorted audio.
     */
    constant?: boolean;
    /**
     * The track selector used when choosing the video track for playback.
     *
     * @docs https://docs.mistserver.org/mistserver/concepts/track_selectors/
     */
    videoTrackSelector?: VideoTrackSelector;
    /**
     * The track selector used when choosing the audio track for playback.
     *
     * @docs https://docs.mistserver.org/mistserver/concepts/track_selectors/
     */
    audioTrackSelector?: AudioTrackSelector;
    /**
     * The timeout of the time to wait for WebRTC `canPlay`, in ms.
     *
     * @default 7000
     */
    canPlayTimeout?: number;
};
/**
 * Performs the actual SDP exchange.
 *
 * 1. Sends the SDP offer to the server,
 * 2. Awaits the server's offer.
 *
 * SDP describes what kind of media we can send and how the server and client communicate.
 *
 * https://developer.mozilla.org/en-US/docs/Glossary/SDP
 * https://www.ietf.org/archive/id/draft-ietf-wish-whip-01.html#name-protocol-operation
 */
export declare function negotiateConnectionWithClientOffer(peerConnection: RTCPeerConnection | null | undefined, endpoint: string | null | undefined, ofr: RTCSessionDescription | null, controller: AbortController, config?: WebRTCVideoConfig, accessControl?: AccessControlParams): Promise<Date>;
/**
 * Constructs the client's SDP offer
 *
 * SDP describes what kind of media we can send and how the server and client communicate.
 *
 * https://developer.mozilla.org/en-US/docs/Glossary/SDP
 * https://www.ietf.org/archive/id/draft-ietf-wish-whip-01.html#name-protocol-operation
 */
export declare function constructClientOffer(peerConnection: RTCPeerConnection | null | undefined, endpoint: string | null | undefined): Promise<RTCSessionDescription | null>;
export declare function getRedirectUrl(endpoint: string, abortController: AbortController, timeout?: number): Promise<URL | null>;
/**
 * Parses the ICE servers from the `Link` headers returned during SDP negotiation.
 */
