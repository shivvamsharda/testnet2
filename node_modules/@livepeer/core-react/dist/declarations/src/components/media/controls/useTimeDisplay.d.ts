import { MediaControllerState } from '@livepeer/core';
type TimeDisplayStateSlice = Pick<MediaControllerState, 'duration' | 'progress' | 'live'>;
type TimeDisplayCoreProps = TimeDisplayStateSlice;
export declare const useTimeDisplay: (props: TimeDisplayCoreProps) => {
    title: string;
    live: boolean;
};
export {};
